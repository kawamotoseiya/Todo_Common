<div class="container">
	<h2>ユーザー編集画面</h2>
	<div class="row">
		<%= form_for @user do |f| %>
		<div class="col-xs-4">
			<%= attachment_image_tag @user, :image, class: "img-square", fallback: "no_image.jpg", size:"200x200" %>
			<%= f.label :イメージ画像 %>
			<%= f.attachment_field :image %>
		</div>
		<div class="col-xs-8">
			<%= f.label :ユーザーネーム %>
			<%= f.text_field :name %>
		</div>
		<div>
			<%= f.submit "ユーザー情報更新" ,class: "btn btn-primary"%>
		</div>
		<% end %>
	</div>
	<div class="row">
		<%= link_to "戻る", :back %>
	</div>
</div>